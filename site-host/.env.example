# Fresh secret for the publisher's Bearer auth. Generate with:  openssl rand -hex 32
# The SAME value must go in the app's .env.local as SITE_PUBLISH_TOKEN.
SITE_PUBLISH_TOKEN=

# Writer API port (published; restrict to the app IP in the EC2 security group).
PUBLISH_PORT=8090
