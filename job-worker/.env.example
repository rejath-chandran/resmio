# Copy to .env for docker-compose. App reads the same DB via EC2_JOBS_DATABASE_URL.
POSTGRES_USER=jobs
POSTGRES_PASSWORD=change-me
POSTGRES_DB=jobs
DB_PORT=5432
EMBED_PORT=8080
SCRAPE_INTERVAL_HOURS=6
